<div class="Sort">
    <div class="Sort-title">{{ __('catalog.sort.sort_by') }}:
    </div>
    <div class="Sort-variants">
        <a class="Sort-sortBy" href="#">{{ __('catalog.sort.price') }}</a>
        <a class="Sort-sortBy Sort-sortBy_inc" href="#">{{ __('catalog.sort.date') }}</a>
    </div>
</div>
